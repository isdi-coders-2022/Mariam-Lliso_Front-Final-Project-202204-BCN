import styled from "styled-components";

const EstablishmentPageStyle = styled.div`
  padding: 1.5rem clamp(1rem, 5%, 3rem);

  .loading {
    display: flex;
    justify-content: center;
  }
`;
export default EstablishmentPageStyle;
